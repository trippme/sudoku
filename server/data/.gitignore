# Never commit the live database.
*.sqlite
*.sqlite3
*.db

# Never commit the Firebase service-account key (a secret) or the cached token —
# including any backup copies (.old/.bak) left behind by a key rotation.
fcm-service-account*.json*
fcm-access-token.json

# The shared backend API key (injected into the app at build time).
api-key.txt
